# Runtime dependencies for the deployed service only.
#
# requirements.txt is the full development freeze and pulls in datasets,
# pyarrow, pandas and pytest — all of which belong to the offline indexing
# scripts and the test suite, not to the running service. Installing those in
# the image costs about a gigabyte for code that never executes in production.
#
# torch is deliberately absent: the Dockerfile installs the CPU-only wheel from
# PyTorch's own index first, because the default PyPI wheel bundles CUDA and is
# roughly 2 GB larger for a machine with no GPU.

fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1

sentence-transformers==4.1.0
transformers==4.57.6

qdrant-client==1.18.0
asyncpg==0.31.0
