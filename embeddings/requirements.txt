# Runtime dependencies for the deployed service. requirements-dev.txt adds the
# test suite and the offline indexing scripts on top of this.
#
# torch is pinned per platform. A bare `torch==2.12.0` lets pip resolve the
# PyPI wheel, which bundles CUDA kernels for a box with no GPU — roughly 2 GB
# and a much longer build. The `+cpu` build exists only for Linux; macOS wheels
# are CPU/MPS already and have no such variant.

--extra-index-url https://download.pytorch.org/whl/cpu

torch==2.12.0+cpu ; sys_platform == "linux"
torch==2.12.0 ; sys_platform != "linux"

sentence-transformers==4.1.0
transformers==4.57.6

fastapi==0.136.3
uvicorn==0.48.0
pydantic==2.13.4
python-dotenv==1.2.2
httpx==0.28.1

qdrant-client==1.18.0
asyncpg==0.31.0
